<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.mycompany</groupId>
  <artifactId>MD_ProyectoFinal</artifactId>
  <version>1.0-SNAPSHOT</version>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>11</maven.compiler.source>
    <maven.compiler.target>11</maven.compiler.target>

    <!-- versiones de plugins -->
    <plugin.compiler.version>3.11.0</plugin.compiler.version>
    <!-- Usa 3.21.2 para PMD 6.x (compatible con tu ruleset) -->
    <plugin.pmd.version>3.21.2</plugin.pmd.version>
    <plugin.spotbugs.version>4.8.6.5</plugin.spotbugs.version>
    <plugin.javafx.version>0.0.4</plugin.javafx.version>
    <findsecbugs.version>1.12.0</findsecbugs.version>
  </properties>

  <dependencies>
    <!-- JavaFX (como en tu POM original) -->
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-controls</artifactId>
      <version>13</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-fxml</artifactId>
      <version>13</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-base</artifactId>
      <version>13</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-graphics</artifactId>
      <version>13</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Compilador: Java 11 y limita compilación a src/.../modelo/** -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${plugin.compiler.version}</version>
        <configuration>
          <source>${maven.compiler.source}</source>
          <target>${maven.compiler.target}</target>
          <includes>
            <include>**/modelo/**</include>
          </includes>
        </configuration>
      </plugin>

      <!-- JavaFX (opcional para correr la app localmente) -->
      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>${plugin.javafx.version}</version>
        <configuration>
          <mainClass>com.mycompany.md_proyectofinal.App</mainClass>
        </configuration>
      </plugin>

      <!-- PMD (PMD 6.x) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
        <version>${plugin.pmd.version}</version>
        <configuration>
          <printFailingErrors>true</printFailingErrors>
          <linkXRef>false</linkXRef>
          <rulesets>
            <!-- este archivo debe existir en MD_ProyectoFinal/config/pmd/pmd-ruleset.xml -->
            <ruleset>config/pmd/pmd-ruleset.xml</ruleset>
          </rulesets>
        </configuration>
      </plugin>

      <!-- SpotBugs + FindSecBugs -->
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>${plugin.spotbugs.version}</version>
        <configuration>
          <effort>max</effort>
          <threshold>Low</threshold>
          <!-- Analiza solo las clases del paquete modelo -->
          <onlyAnalyze>modelo.*</onlyAnalyze>
          <plugins>
            <plugin>
              <groupId>com.h3xstream.findsecbugs</groupId>
              <artifactId>findsecbugs-plugin</artifactId>
              <version>${findsecbugs.version}</version>
            </plugin>
          </plugins>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
